# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# Firebase Admin SDK Configuration
# The base64 encoded JSON string of the Firebase service account key
# You can generate this in Firebase Console -> Project Settings -> Service Accounts
FIREBASE_SERVICE_ACCOUNT_BASE64=""

# (Optional) Explicit Firestore Database ID to connect to
# If not provided, the SDK connects to the "(default)" database.
FIRESTORE_DATABASE_ID="roy-share"

# Firebase Client Configuration for Dashboard Login
VITE_FIREBASE_API_KEY=""
VITE_FIREBASE_AUTH_DOMAIN=""
VITE_FIREBASE_PROJECT_ID=""
VITE_FIREBASE_STORAGE_BUCKET=""
VITE_FIREBASE_MESSAGING_SENDER_ID=""
VITE_FIREBASE_APP_ID=""
